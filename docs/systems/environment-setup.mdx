---
sidebar_position: 1
---

import TranslationToggle from '@site/src/components/TranslationToggle';

<TranslationToggle contentId="environment-setup">

# Setting Up the Development Environment

## Hero Insight
Transform your development workflow with a properly configured environment that enables efficient AI-powered documentation development.

## Purpose of This Chapter
You will set up a complete development environment for building AI-powered documentation systems, including Docusaurus, FastAPI, Qdrant integration, and all necessary tools for the project.

## Deep Breakdown

### Prerequisites
Before starting, ensure you have:

#### System Requirements
- **Operating System**: Windows, macOS, or Linux
- **Node.js**: Version 18.x or higher for Docusaurus
- **Python**: Version 3.9 or higher for FastAPI backend
- **Git**: For version control and repository management
- **Package Managers**: npm/yarn for frontend, pip for backend

#### Development Tools
- **Code Editor**: VS Code with Python and JavaScript extensions
- **Terminal**: Command line interface for project management
- **Browser**: Modern browser for testing and development
- **API Client**: Postman or similar for API testing

### Installing Node.js and npm
For Docusaurus development:

```bash
# Check current versions
node --version
npm --version

# Install Node.js (includes npm)
# Visit nodejs.org and download LTS version
# Or use a version manager like nvm
```

### Installing Python and pip
For FastAPI backend development:

```bash
# Check current versions
python --version
pip --version

# Install Python 3.9+ from python.org
# Or use a version manager like pyenv
```

### Project Directory Structure
Create the project with proper organization:

```
ai-book-project/
├── frontend/           # Docusaurus documentation site
│   ├── docs/          # Documentation content
│   ├── src/           # Custom React components
│   ├── static/        # Static assets
│   └── package.json   # Frontend dependencies
├── backend/           # FastAPI backend services
│   ├── app/          # Application code
│   ├── models/       # Data models
│   ├── routes/       # API routes
│   └── requirements.txt # Backend dependencies
├── .gitignore        # Git ignore file
├── README.md         # Project documentation
└── package.json      # Root package file (optional)
```

### Setting Up Docusaurus Frontend
Create the Docusaurus documentation site:

```bash
# Navigate to project directory
cd ai-book-project

# Create frontend directory
mkdir frontend && cd frontend

# Initialize Docusaurus project
npx create-docusaurus@latest website classic

# Install additional dependencies
npm install @docusaurus/module-type-aliases @docusaurus/types
```

### Setting Up FastAPI Backend
Create the FastAPI backend services:

```bash
# Navigate back and create backend directory
cd ..
mkdir backend && cd backend

# Create virtual environment
python -m venv venv

# Activate virtual environment
# On Windows:
venv\Scripts\activate
# On macOS/Linux:
source venv/bin/activate

# Create requirements.txt
echo "fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
python-dotenv==1.0.0
qdrant-client==1.7.0
openai==1.3.5
tiktoken==0.5.2" > requirements.txt

# Install dependencies
pip install -r requirements.txt
```

### Environment Configuration
Set up environment variables for API keys and configuration:

```bash
# Create .env file in backend directory
echo "OPENAI_API_KEY=your_openai_api_key_here
QDRANT_URL=your_qdrant_cluster_url
QDRANT_API_KEY=your_qdrant_api_key
ENVIRONMENT=development" > .env
```

### Creating Basic Backend Structure
Set up the basic FastAPI application:

```bash
# Create the basic app structure
mkdir -p app/{models,routes,services,utils}
touch app/main.py app/config.py app/schemas.py
```

### Docusaurus Configuration
Update the Docusaurus configuration for your project:

**frontend/docusaurus.config.js:**
```javascript
// @ts-check
const lightCodeTheme = require('prism-react-renderer/themes/github');
const darkCodeTheme = require('prism-react-renderer/themes/dracula');

/** @type {import('@docusaurus/types').Config} */
const config = {
  title: 'AI-Powered Documentation',
  tagline: 'Learn with intelligent, context-aware documentation',
  favicon: 'img/favicon.ico',

  url: 'https://your-username.github.io',
  baseUrl: '/ai-book/',

  organizationName: 'your-organization',
  projectName: 'ai-book',

  onBrokenLinks: 'throw',
  onBrokenMarkdownLinks: 'warn',

  i18n: {
    defaultLocale: 'en',
    locales: ['en'],
  },

  presets: [
    [
      'classic',
      /** @type {import('@docusaurus/preset-classic').Options} */
      ({
        docs: {
          sidebarPath: require.resolve('./sidebars.js'),
          editUrl: 'https://github.com/your-repo/ai-book/',
        },
        blog: false, // Disable blog for now
        theme: {
          customCss: require.resolve('./src/css/custom.css'),
        },
      }),
    ],
  ],

  themeConfig:
    /** @type {import('@docusaurus/preset-classic').ThemeConfig} */
    ({
      image: 'img/docusaurus-social-card.jpg',
      navbar: {
        title: 'AI Book',
        logo: {
          alt: 'AI Book Logo',
          src: 'img/logo.svg',
        },
        items: [
          {
            type: 'docSidebar',
            sidebarId: 'tutorialSidebar',
            position: 'left',
            label: 'Documentation',
          },
          {
            href: 'https://github.com/your-repo/ai-book',
            label: 'GitHub',
            position: 'right',
          },
        ],
      },
      footer: {
        style: 'dark',
        links: [
          {
            title: 'Docs',
            items: [
              {
                label: 'Documentation',
                to: '/docs/intro',
              },
            ],
          },
          {
            title: 'More',
            items: [
              {
                label: 'GitHub',
                href: 'https://github.com/your-repo/ai-book',
              },
            ],
          },
        ],
        copyright: `Copyright © ${new Date().getFullYear()} AI-Powered Documentation Project`,
      },
      prism: {
        theme: lightCodeTheme,
        darkTheme: darkCodeTheme,
      },
    }),
};

module.exports = config;
```

### Basic FastAPI Application
Create the main FastAPI application:

**backend/app/main.py:**
```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from dotenv import load_dotenv
import os

# Load environment variables
load_dotenv()

app = FastAPI(
    title="AI Documentation Backend",
    description="Backend services for AI-powered documentation system",
    version="1.0.0"
)

# Configure CORS for local development
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],  # In production, specify exact origins
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

@app.get("/")
async def root():
    return {"message": "AI Documentation Backend API"}

@app.get("/health")
async def health_check():
    return {"status": "healthy"}

if __name__ == "__main__":
    import uvicorn
    uvicorn.run(app, host="0.0.0.0", port=8000)
```

### Development Workflow Setup
Configure your development workflow:

#### Git Configuration
```bash
# Initialize git repository
git init

# Create .gitignore
echo "node_modules/
venv/
.env
__pycache__/
*.pyc
.DS_Store
dist/
build/" > .gitignore

# Add and commit initial files
git add .
git commit -m "Initial project setup"
```

#### Package.json for Root
Create a root package.json for easier management:

**package.json:**
```json
{
  "name": "ai-book-project",
  "version": "1.0.0",
  "description": "AI-powered documentation system",
  "scripts": {
    "frontend:dev": "cd frontend && npm run start",
    "frontend:build": "cd frontend && npm run build",
    "backend:dev": "cd backend && python -m uvicorn app.main:app --reload --port 8000",
    "backend:prod": "cd backend && python -m uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "setup": "npm install && cd frontend && npm install && cd ../backend && pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
```

### Testing the Setup
Verify that your environment is properly configured:

```bash
# Test frontend setup
cd frontend
npm run build

# Test backend setup
cd ../backend
python -c "import fastapi; print('FastAPI imported successfully')"
```

## 🎨 UI/UX Angle
A well-structured development environment enables rapid iteration on UI components and seamless integration between frontend and backend services, leading to better user experiences.

## 📘 Real-World Example
A properly configured environment allows you to:
- Develop frontend components that interact with backend APIs
- Test RAG functionality with real data
- Iterate quickly on user interface improvements
- Deploy changes efficiently to production

## ⚠️ Common Mistakes & Fixes

### Mistake: Not using virtual environments for Python
**Fix**: Always create and activate a virtual environment before installing Python packages.

### Mistake: Exposing API keys in version control
**Fix**: Use .env files and ensure they're included in .gitignore.

### Mistake: Inconsistent port configurations
**Fix**: Document and standardize port assignments for different services.

## 🧪 Micro-Exercises

1. **Exercise**: Set up the complete development environment following the steps above.
2. **Exercise**: Verify that both frontend and backend can run independently.

## 🧭 Reflection Prompts
- How does your current development environment compare to this setup?
- What improvements could you make to your development workflow?
- How might a well-structured environment impact your productivity?

## End-of-Chapter Summary
- Proper development environment setup is crucial for project success
- Both frontend and backend need specific configurations
- Environment variables should be properly secured
- Version control should be configured from the start
- The setup enables efficient development of AI-powered systems

</TranslationToggle>